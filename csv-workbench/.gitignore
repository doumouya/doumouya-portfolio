# Rust build artifacts
/target
**/target

# TS build intermediates (regenerated by tools/build.sh).
# The built site under web/ (app.js, tokens.css, wasm/) IS committed — Pages serves it directly.
/node_modules
/.build
*.tsbuildinfo

# local env / OS cruft
.env
.env.*
*.swp
*~
.DS_Store
