# specs

One spec doc per feature/Case (`<slug>.md`), written by the **architect** — the source of truth the
tester, coder, and reviewer build against. Structure: problem/intent · numbered acceptance criteria
(tests map 1:1) · exact API contracts · scope boundaries · risks & open questions. Mirrors a Case
when the Cases MCP is wired; until then this file *is* the Case.
