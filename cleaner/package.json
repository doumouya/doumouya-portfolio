{
  "name": "cleaner-web",
  "version": "0.1.0",
  "description": "The Cleaner — SpreadSheet Paper reborn on amenan-ui (Console theme) + the csv-workbench Polars-wasm engine + a birama-engine metadata plane.",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "sh tools/build.sh"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "typescript": "^5.6.3"
  }
}
